#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 1; (i) <= (n); ++(i))
#define ll long long 

int main() {
    ll int n ,k;
    cin >> n >> k;
    ll int p[n];
    rep(i,n){
        cin >> p[i];
    }
    for(int i = 1; i <= n; i++){
        
    }
}

